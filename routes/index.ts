export default eventHandler(async () => {
  
	// await useStorage('redis').setItems([{ key: 'bar', value: 'foobar' }]);
	// return { nitro: await useStorage('redis').getItem<{ hello: string }>('foo') };
});
